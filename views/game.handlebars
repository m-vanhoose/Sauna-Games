<h1 class="game-title" id="game-page-title">{{game.title}}</h1>

<div><img id="game-page-img" src="{{game.image}}" alt="{{game.title}}"></div>

<div id= "game-page-details-card">
    <div class="game-description">{{game.description}}</div>
    <div class="price "id="game-page-price">{{game.price}}</div>
</div>

<div id="button-container">
    <button id="saveGame" class ="library-button" data-gameId={{game.id}}>Add to Library</button>
</div>
<script src="../js/button.js"></script> 
