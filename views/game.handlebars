<h1>{{game.title}}</h1>

<div><img src="{{game.image}}" alt="{{game.title}}"></div>

<div>{{game.description}}</div>

<button id = "saveGame" data-gameId={{game.id}}>Add to Library</button>



<div>{{game.price}}</div>
</div>

<script src="/js/button.js"></script>